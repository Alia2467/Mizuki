<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 主主题：纯白背景、无系统标题栏（MaterialComponents 以支持圆角图片组件） -->
    <style name="Theme.MIzuki" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/text_primary</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/text_primary</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 圆角图片（图2保持原比例、圆角） -->
    <style name="RoundedImage">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>
</resources>
